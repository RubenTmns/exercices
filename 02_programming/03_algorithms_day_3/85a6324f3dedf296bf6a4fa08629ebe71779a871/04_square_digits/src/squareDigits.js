function squareDigits(x) {
  // Code here
  if (Number.isInteger(x)) {
    let result = x * x;
    return result;
  } 
  throw !(Number.isInterger(x));
   
  

 


  


    

 
};

// Leave the line below for tests to work
module.exports = squareDigits;
